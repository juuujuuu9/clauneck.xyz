# Secret salt for hashing user input in the /api/hash endpoint
# Generate a secure random value: openssl rand -hex 32
# Required in production - do not use the default fallback value
SIGIL_SECRET_SALT=your-secret-salt-here-change-in-production

# NODE_ENV is automatically set by Vercel (production/development)
# No need to set this manually
